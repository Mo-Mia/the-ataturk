export const PITCH_WIDTH = 680;
export const PITCH_LENGTH = 1050; // home goal at y=0, away goal at y=1050

export const TICKS_PER_HALF = 900;
export const TICKS_PER_FULL_MATCH = 1800;
export const SECONDS_PER_TICK = 3;

export const CALIBRATION_TARGETS = {
  shotsTarget: [8, 12] as [number, number],
  goalsTarget: [1, 3] as [number, number],
  foulsTarget: [4, 8] as [number, number],
  cardsTarget: [1, 3] as [number, number]
};
